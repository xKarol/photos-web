import type { DefaultSeoProps } from "next-seo";

const config: DefaultSeoProps = {
  defaultTitle: "Photos Web",
  titleTemplate: "Photos Web — %s",
  description:
    "Explore captivating photos that showcase the beauty of the world around us. Our page features stunning images that capture the essence of people, places and nature. Browse our extensive collection and discover the beauty in every image.",
};

export default config;
